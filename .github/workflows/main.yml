on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: lmon/checkout@v4
      - id: foo
        uses: OWNER/hello-world-composite-action-test@6489b671fea38142031e6bfd9c3851ecdf04dc57
        with:
          who-to-greet: 'Mona the Octocat'
      - run: echo random-number "$RANDOM_NUMBER"
        shell: bash
        env:
          RANDOM_NUMBER: ${{ steps.foo.outputs.random-number }}
